{
  "name": "clawnboard",
  "version": "0.1.0",
  "private": true,
  "description": "Local dashboard for deploying OpenClaw AI agents (moltbots) on Fly.io",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "setup": "bash scripts/setup.sh"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
